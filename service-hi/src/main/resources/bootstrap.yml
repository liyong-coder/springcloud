server:
  port: 8763
  #port: 8762

spring:
  application:
    name: SERVICE-HI
  profiles:
    active: dev
  cloud:
    config:
      profile: ${spring.profiles.active}
      name: ${spring.application.name}
      enabled: true
      discovery:
        enabled: true
        service-id: CONFIG-SERVICE







