server:
  port: 8763
  #port: 8762

spring:
  application:
    name: SERVICE-HI
  profiles:
    active: dev
  cloud:
    config:
      profile: ${spring.profiles.active}
      name: ${spring.application.name}
      enabled: true
      discovery:
        enabled: true
        service-id: CONFIG-SERVICE
#    bus:
#      enabled: true
#      trace:
#        enabled: true
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
#management:
#  endpoints:
#    web:
#      exposure:
#        include: bus-refresh






